import React from "react";

const Tile = (props) => {
    return (
        <div className="tile" style={{ paddingTop: "100%" }}>
            {/* stuff here */}
            {/* blah */}
        </div>
    );
};

export default Tile;
